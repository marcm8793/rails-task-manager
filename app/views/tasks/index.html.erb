<h1>My Tasks</h1>
<%= link_to "Add a task", new_task_path %>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= task.completed ? "✅" : "❌" %>
      <%= task.title %> <%= link_to "see tasks details", task_path(task) %>
    </li>
  <% end %>
</ul>
